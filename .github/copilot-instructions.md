# Copilot Instructions

## 项目指南
- 用户要求在此代码库中：优化启动项读取缓冲与编码、把耗时 IO/枚举移到后台线程、对结束进程与修改注册表操作给出更好错误信息并处理权限、并用 `CListCtrl`（报告视图）替代 `CListBox`。今后应按这些优先项修改代码并提供相应的 UI 变更指导。
- 用户已将 `IDC_LIST1` 和 `IDC_LIST2` 替换为 `CListCtrl`（报告视图），并明确表示不想要刷新按钮（不要实现刷新按钮）。将根据此偏好实现后台刷新等改动。
- 用户偏好："最小化到托盘"每次打开都默认选中且不持久化（不保存到配置文件）。